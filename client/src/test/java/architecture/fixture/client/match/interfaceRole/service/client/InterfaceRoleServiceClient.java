package architecture.fixture.client.match.interfaceRole.service.client;

import architecture.fixture.client.match.interfaceRole.service.InterfaceRoleService;

/** An invalid role fixture because the client role is an interface. */
interface InterfaceRoleServiceClient extends InterfaceRoleService {
}
